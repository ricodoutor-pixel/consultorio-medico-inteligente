/**
 * ManyChatWidget — disabled.
 *
 * The ManyChat embed has been removed from the project. This component is
 * intentionally a no-op so existing imports keep working without breaking
 * the build. WhatsApp / Brisa lead routing is now handled directly by
 * `WhatsAppButton` + analytics events in `index.html`.
 */
export const ManyChatWidget = () => null;

export default ManyChatWidget;
